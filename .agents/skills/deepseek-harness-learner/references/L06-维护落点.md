# L06 · 维护本技能（技能内）

创建时定下的原则，**以后改本技能也一样**。

## 唯一真源

```text
{工作区根}/.agents/skills/deepseek-harness-learner/
```

Cursor 与 Pi 都读工作区 `.agents/skills`。更新、改触发词、改工作流、改 references，都只改这一棵目录。不要在 `.cursor/skills` 或个人家目录再复制一份当第二真源。

`name`、目录名、`SKILL.md` 里的 `name` 保持 `deepseek-harness-learner`。`deepseek-hardness-learner` 只留在 description 作触发别名。

## 改完要同时想的

- 学堂落点仍是 `R20260820-02-学习教程`（见 [L02](./L02-落点与文种.md)）。
- 新增长文：在 [00-索引.md](./00-索引.md) 取下一个 `L` 号，文件名 `L{NN}-{主题}.md`。课文版式见 [L07](./L07-课文版式与双链.md)；图见 [L08](./L08-可视化.md)；标签见 [L09](./L09-标签与重点.md)；外援出图/PPT 见 [L10](./L10-能力路由.md)。
- 旧课不强制按新版式回改。用户追问某一课时，当次把该课补上附录、调查题、标签和图。
- 本技能保持自包含：**冷启动**只读本目录。外援技能的名字、何时 Read、产物落点写在 L10；不要把 `~/.claude/skills/...` 写成每次写课的必读。对方手册仍在对方目录，禁止抄进本包当第二真源。
- 工作区根若提到本技能，链接仍指向 `.agents/skills/deepseek-harness-learner/`。
- 本机若已把技能发布到 `~/.agents` / `~/.claude/skills`：那些是触发副本。改正文只改工作区这一份；课文仍只进学堂夹（[L11](./L11-多端同落点.md)）。不要改全局副本当真源。
